/** Golden-template entry — the Coding Agent replaces this per SPEC.md / SCREENS.md. */
export default function Page() {
  return (
    <main style={{ padding: 24 }}>
      <h1>Factory App</h1>
      <p>Replace me per SPEC.md</p>
    </main>
  );
}
