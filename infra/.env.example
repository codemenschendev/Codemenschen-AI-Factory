# Compose-level secrets (infra/.env — never committed)
POSTGRES_PASSWORD=

# EAS Build — expo.dev → Account settings → Access tokens (robot token).
# Leave empty to skip cloud builds (release stage ships the source bundle only).
EXPO_TOKEN=
# Expo account/organization that owns the appwerk apps (optional)
EXPO_OWNER=
